# 核心依赖
pdf2image>=1.17.0
pytesseract>=0.3.13
Pillow>=10.2.0
PyPDF2>=3.0.0

# Office 文档转换（docx/xlsx/xls/pptx）
markitdown[docx,pptx]>=0.1.5
xlrd>=2.0.1
# pptx 图片 OCR 依赖（python-pptx 已由 markitdown[pptx] 引入）
# pytesseract / Pillow 已在上方核心依赖中

# 可选依赖（用于进度监控）
# tqdm>=4.66.0

# 系统依赖（需要单独安装）
# Tesseract OCR: https://github.com/tesseract-ocr/tesseract
# poppler-utils（用于 pdf2image）
# .doc 转换使用 macOS 自带 textutil（无需安装）
